# event

Event publish/broadcast module with filters and handlers.

Components:

- `Event`, `Declare`, `Filter`, `Handler`

API:

- `event.Publish`, `event.Broadcast`
- `event.PublishTo`, `event.BroadcastTo`
