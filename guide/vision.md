# 设计想法

当前代码体现出的核心设计思路：

- 统一注册：所有能力都通过 `bamgoo.Register(name, value)` 进入系统。
- 生命周期一致：模块都实现相同的生命周期接口。
- 配置驱动：模块优先从全局配置读取实例与驱动信息。
- 默认可用：每个关键模块都内置默认驱动，降低启动门槛。
- 渐进增强：先本地单进程可跑，再切换到 Redis/NATS 等外部基础设施。

这使得文档结构也应保持一致：每个模块都按照 “介绍 -> 配置 -> API -> 示例 -> 驱动” 展开。
